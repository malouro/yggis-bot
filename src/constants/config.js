const commandPrefix = '!'
const statusMessage = `${commandPrefix}help`
const statusMessageOptions = {
	type: 'WATCHING',
	url: '',
}

export default ({
	commandPrefix,
	statusMessage,
	statusMessageOptions,
})
